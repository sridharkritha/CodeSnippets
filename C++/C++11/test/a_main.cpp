#include <iostream>
using namespace std;




/*
int main(int argc, char *argv[]) {
	// for(int i = 1; i < argc; ++i) printf("%s%s", argv[i], (i < argc-1) ? " ": "");

	//for(int i = 1; i < argc; ++i) printf("%s%s", *++argv, (i < argc-1) ? " ": "");

	
	// for (int i = 0; i < 3; ++i) {
	//	cout << (*++argv)[0] << endl; // h w s
	// }


	cout << (*++argv)[0] << endl; // h
	for(int i = 0; i < 3; ++i) {
	cout << *++argv[0] << endl; // e l l
	}
	cout << ".........................";
	cout << (*++argv)[0] << endl; // w
	for (int i = 0; i < 3; ++i) {
		cout << *++argv[0] << endl; // o r l
	}
	// (*++argv)[0] = h
	// *++argv[0] = e
	return 0;
}
	*/
